import 'package:roll_dice/gradient_container.dart';
import 'package:flutter/material.dart';

void main() {
  runApp(const GradientContainer([
              Color.fromARGB(255, 76, 10, 123),
              Color.fromARGB(255, 0, 0, 0)
            ]
          )
        );
}
